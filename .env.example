# Environment Variables Template for TradeX
# Copy this file to .env for local development
# For production (Render.com), set these in the Render dashboard

# Django Secret Key (Generate a new one for production!)
# You can generate one using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here

# Debug Mode (Set to False in production)
DEBUG=True

# Database URL (Render will auto-populate this for production)
# For local development with PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# For local development with MySQL (current setup):
# DATABASE_URL=mysql://root:password@localhost:3306/Dhan
DATABASE_URL=

# Allowed Hosts (comma-separated, Render will auto-populate)
ALLOWED_HOSTS=localhost,127.0.0.1

# Python Version (for Render.com)
PYTHON_VERSION=3.11.0
